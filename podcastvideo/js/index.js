lottie.loadAnimation({
    container: silver, // the dom element that will contain the animation
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: '../js/portfolio_silverribbon_animation/data.json' // the path to the animation json
});