// CAMERA ANIMATION

lottie.loadAnimation({
    container: camera, // the dom element that will contain the animation
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: 'js/portfolio_camera_animation/data.json' // the path to the animation json
});

// CAMERA ANIMATION

lottie.loadAnimation({
    container: scrolling, // the dom element that will contain the animation
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: 'js/portfolio_scroll_animation/data.json' // the path to the animation json
});

// COG ANIMATION

lottie.loadAnimation({
    container: cog, // the dom element that will contain the animation
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: 'js/portfolio_cog_animation/data.json' // the path to the animation json
});

// BIO ANIMATION

lottie.loadAnimation({
    container: bio, // the dom element that will contain the animation
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: 'js/portfolio_bio_animation/data.json' // the path to the animation json
});

// WELCOME ANIMATION

lottie.loadAnimation({
    container: heroanimation, // the dom element that will contain the animation
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: 'js/portfolio_hero_animation/data.json' // the path to the animation json
});