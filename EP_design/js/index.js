lottie.loadAnimation({
    container: gold, // the dom element that will contain the animation
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: '../js/portfolio_goldribbon_animation/data.json' // the path to the animation json
});