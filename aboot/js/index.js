lottie.loadAnimation({
    container: aboot, // the dom element that will contain the animation
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: '../js/portfolio_aboot_animation/data.json' // the path to the animation json
});